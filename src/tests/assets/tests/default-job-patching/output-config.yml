jobs:
  another-failing-job:
    docker:
    - image: cimg/base
    steps:
    - run: exit 1
  failing-job:
    docker:
    - image: cimg/base
    steps:
    - run: exit 1
  orb-conditional-job-succeed:
    docker:
    - image: cimg/base
    steps:
    - run: exit 0
  succeeding-job:
    docker:
    - image: cimg/base
    steps:
    - run: exit 0
version: 2.1
workflows:
  fail:
    jobs:
    - failing-job:
        context: global
        name: failing-job
        type: approval
    - failing-job:
        context: global
        name: failing-job
        type: approval
  run-succeed-and-fail:
    jobs:
    - succeeding-job
    - failing-job:
        context: global
        name: failing-job
        requires:
        - succeeding-job
        type: approval
  succeed:
    jobs:
    - succeeding-job
    - another-failing-job:
        context: global
        name: failing-job
        type: approval
  version: 2
